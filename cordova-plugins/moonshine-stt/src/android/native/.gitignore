# Native build intermediates — regenerated by build.mjs via NDK
build/

# Produced native libraries (built from the vendored transcribe.cpp source
# by `npm run build:native`). Not tracked: the repo ships reproducible
# source, not prebuilt binaries (F-Droid requirement).
libs/