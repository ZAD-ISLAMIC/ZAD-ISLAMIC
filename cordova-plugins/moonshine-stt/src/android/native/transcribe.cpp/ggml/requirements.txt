# This file is intentionally left without package pins.
#
# The actual dev-only build dependencies for converting the Moonshine STT
# model (tensorflow, torch, transformers, keras, accelerate, …) live in
# ggml-model-build-deps.txt. They are used solely to produce the GGUF model
# and are NOT shipped in the Android APK, so they are kept out of the
# dependency graph to avoid false-positive security alerts.
#
# To set up the local conversion environment:
#   pip install -r ggml-model-build-deps.txt
